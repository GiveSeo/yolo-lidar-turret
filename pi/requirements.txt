# Raspberry Pi 5 노드 의존성
#
# ⚠️ 중요: Pi 에서는 opencv 와 numpy 를 pip 로 설치하지 말 것!
#    picamera2(apt 설치)가 시스템 numpy 에 빌드되어 있어, pip 로 다른 numpy/opencv 를
#    깔면 ABI 충돌로 "Bus error" / segfault 가 난다.
#
# 권장 설치 (시스템 패키지 + 최소 pip):
#    sudo apt install -y python3-picamera2 python3-opencv python3-numpy python3-serial
#    python3 -m venv --system-site-packages venv   # 시스템 패키지 상속
#    source venv/bin/activate
#    pip install -r requirements.txt               # 아래 pyserial 만(이미 apt 로 깔았으면 생략 가능)

pyserial>=3.5

# opencv-python  <- 설치 금지(시스템 python3-opencv 사용)
# numpy          <- 설치 금지(시스템 python3-numpy 사용; picamera2 와 ABI 일치 필요)
